/**
 * Created by lle on 9/15/14.
 */
module.exports = function (grunt) {
    grunt.loadNpmTasks('grunt-ngdocs');
    grunt.loadNpmTasks('grunt-contrib-connect');
    grunt.loadNpmTasks('grunt-contrib-clean');
    grunt.initConfig({
        ngdocs: {
            options: {
                html5Mode: false,
		title: 'Page Object'
            },
	    core: {
                title:'Core',
                startPage: '/docs/protractor-core',
                src: ['./commons/*.js']
            }
        },
        connect: {
            options: {
                keepalive: true,
                port:8002,
                hostname:'*'
            },
            server: {}
        },
        clean: ['docs']
    });
    grunt.registerTask('default', ['clean', 'ngdocs', 'connect']);
};

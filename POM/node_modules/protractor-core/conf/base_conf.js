/**
 * General base configurations to be used by other configuration file.
 */
var baseConf = function () {

  this.timeout= {
    global: 20000,
    pageLoad: 120000,
    implicitWait: 15000,
    angularDelay: 1000,
    defaultTimeoutInterval: 100000,
    allScriptsTimeout: 120000
  };

};
module.exports = baseConf;	//naming the page object
